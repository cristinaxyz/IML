Training the model:

```uv run main.py```

Analyzing the training logs (for peak validation accuracy and loss, and to plot the losses over time):

```uv run log_analysis.py filename.log```

Computing metrics and generating figures:

```uv run evaluate.py```
